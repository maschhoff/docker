<?xml version="1.0"?>
<Container version="2">
  <Name>RsyncWebUI</Name>
  <Repository>rsyncwebui:latest</Repository>
  <Registry/>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support/>
  <Project/>
  <Overview>
    RsyncWebUI is a web interface for rsync. Pick one or more sources and a destination from a
    file browser, tick the rsync options you want, save it as a task, and let it repeat on a
    cron schedule. Every run shows a live log with a progress bar and is kept in a history.

    Note: the container needs read and write access to the shares it backs up. By default
    /mnt/user and /mnt/disks are mounted.
  </Overview>
  <Category>Backup: Tools:Utilities</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <TemplateURL/>
  <Icon>https://icons.iconarchive.com/icons/papirus-team/papirus-apps/256/realtimesync-icon.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled>0</DateInstalled>
  <DonateText/>
  <DonateLink/>
  <Requires/>

  <Config Name="WebUI Port"
          Target="8080"
          Default="8080"
          Mode="tcp"
          Description="Port of the web interface."
          Type="Port"
          Display="always"
          Required="true"
          Mask="false">8080</Config>

  <Config Name="Configuration"
          Target="/config"
          Default="/mnt/user/appdata/rsyncwebui"
          Mode="rw"
          Description="Database of tasks and run history. An ssh directory with SSH keys can live here too."
          Type="Path"
          Display="always"
          Required="true"
          Mask="false">/mnt/user/appdata/rsyncwebui</Config>

  <Config Name="Shares (/mnt/user)"
          Target="/mnt/user"
          Default="/mnt/user"
          Mode="rw"
          Description="Access to the Unraid shares. Sources and destinations are picked from here."
          Type="Path"
          Display="always"
          Required="true"
          Mask="false">/mnt/user</Config>

  <Config Name="Unassigned Devices (/mnt/disks)"
          Target="/mnt/disks"
          Default="/mnt/disks"
          Mode="rw,slave"
          Description="External drives from Unassigned Devices as a backup destination. The rw,slave mode makes drives plugged in later visible inside the container."
          Type="Path"
          Display="always"
          Required="false"
          Mask="false">/mnt/disks</Config>

  <Config Name="Remote Shares (/mnt/remotes)"
          Target="/mnt/remotes"
          Default="/mnt/remotes"
          Mode="rw,slave"
          Description="Mounted SMB or NFS shares from Unassigned Devices."
          Type="Path"
          Display="advanced"
          Required="false"
          Mask="false"/>

  <Config Name="Timezone"
          Target="TZ"
          Default="Europe/Berlin"
          Mode=""
          Description="Timezone used for schedules. Should match the server timezone."
          Type="Variable"
          Display="always"
          Required="true"
          Mask="false">Europe/Berlin</Config>

  <Config Name="Allowed roots"
          Target="BROWSE_ROOTS"
          Default="/mnt"
          Mode=""
          Description="Comma-separated paths the file browser may show. Anything outside is rejected."
          Type="Variable"
          Display="advanced"
          Required="true"
          Mask="false">/mnt</Config>

  <Config Name="Username (optional)"
          Target="AUTH_USER"
          Default=""
          Mode=""
          Description="Enables simple access protection. Leave empty for no login."
          Type="Variable"
          Display="always"
          Required="false"
          Mask="false"/>

  <Config Name="Password (optional)"
          Target="AUTH_PASS"
          Default=""
          Mode=""
          Description="Password matching the username above."
          Type="Variable"
          Display="always"
          Required="false"
          Mask="true"/>

  <Config Name="Port (internal)"
          Target="PORT"
          Default="8080"
          Mode=""
          Description="Port the web server listens on inside the container."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false">8080</Config>
</Container>
